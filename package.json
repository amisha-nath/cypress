{
  "devDependencies": {
    "cypress": "^15.2.0",
    "cypress-mochawesome-reporter": "^4.0.2",
    "mocha": "^11.7.2",
    "mochawesome": "^7.1.4",
    "mochawesome-merge": "^5.0.0"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:chrome": "cypress run --browser chrome",
    "test": "cypress run --browser chrome --headless",
    "test:report": "npm test && npx mochawesome-merge cypress/reports/*.json > cypress/reports/report.json && npx marge cypress/reports/report.json -f report -o cypress/reports/html"
  }
}
